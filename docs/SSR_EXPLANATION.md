# Объяснение: Почему виден JSON в data-page, но SSR работает

## 🔍 Важное понимание

Когда вы видите в исходном коде страницы:
```html
<div id="app" data-page="{...json...}">
```

Это **НЕ означает**, что SSR не работает!

## ✅ Как работает SSR в Laravel Inertia

Laravel Inertia использует **гибридный подход**:

1. **SSR рендерит HTML контент** внутри `<div id="app">`
2. **Атрибут `data-page`** содержит JSON данные для гидратации на клиенте
3. **Оба присутствуют одновременно** - это нормально!

### Правильная структура с SSR:

```html
<div id="app" data-page='{"component":"CatalogPage","props":{...}}'>
    <!-- ВОТ ТУТ ДОЛЖЕН БЫТЬ HTML КОНТЕНТ! -->
    <header>...</header>
    <main>...</main>
    <section>...</section>
    <!-- Весь контент виден в исходном HTML -->
</div>
```

### Неправильная структура (без SSR):

```html
<div id="app" data-page='{"component":"CatalogPage","props":{...}}'>
    <!-- Пусто, контент отсутствует -->
</div>
```

## 🔍 Как проверить, работает ли SSR

### Способ 1: Проверка через curl

```bash
# Проверка наличия HTML контента ПОСЛЕ открывающего тега
curl -s -A "Googlebot/2.1" http://localhost:8116/catalog | \
  sed -n '/<div id="app"/p' | \
  sed 's/<div id="app"[^>]*>//' | \
  head -c 500
```

**Ожидаемый результат:**
- ✅ **SSR работает**: Видите HTML теги (`<header>`, `<main>`, `<section>` и т.д.)
- ❌ **SSR не работает**: Видите только пустоту или закрывающий `</div>`

### Способ 2: Проверка размера HTML

```bash
curl -s -A "Googlebot/2.1" http://localhost:8116/catalog | wc -c
```

**Ожидаемый результат:**
- ✅ **SSR работает**: Размер > 100KB (обычно 500KB - 2MB)
- ❌ **SSR не работает**: Размер < 50KB

### Способ 3: Подсчет HTML тегов

```bash
curl -s -A "Googlebot/2.1" http://localhost:8116/catalog | \
  grep -cE "(<header|<main|<section|<article|<h1|<h2|<nav)"
```

**Ожидаемый результат:**
- ✅ **SSR работает**: Найдено много тегов (> 10)
- ❌ **SSR не работает**: Найдено 0 тегов

## 📊 Почему в браузере может быть виден только JSON?

### Причина 1: Вы смотрите на атрибут `data-page`

Атрибут `data-page` всегда содержит JSON - это нормально! Это данные для гидратации на клиенте.

**Важно:** HTML контент находится **ПОСЛЕ** открывающего тега `<div id="app">`, а не в атрибуте `data-page`.

### Причина 2: Laravel Inertia использует SSR только для первого запроса

Laravel Inertia может использовать SSR только для:
- Первого запроса страницы (не для AJAX запросов)
- Поисковых ботов (определяется через User-Agent)

Для обычных запросов из браузера может использоваться клиентский рендеринг.

### Причина 3: Кеш браузера

Браузер может показывать кешированную версию без SSR.

**Решение:** Очистите кеш браузера или используйте режим инкогнито.

## ✅ Текущий статус

Судя по проверкам:
- ✅ SSR сервер запущен
- ✅ SSR бандл собран
- ✅ HTML контент рендерится (размер ~1.6 МБ)
- ✅ Внутри `<div id="app">` есть HTML контент

**Вывод:** SSR работает корректно!

## 🎯 Как увидеть HTML контент в браузере

1. **Откройте страницу:** `http://localhost:8116/catalog`
2. **Правый клик → "Просмотр кода страницы" (View Page Source)**
3. **Найдите `<div id="app">`**
4. **Прокрутите вниз** - после открывающего тега должен быть HTML контент

Если вы видите только `data-page` и ничего после него, значит:
- Либо SSR не используется для этого запроса (попробуйте с User-Agent бота)
- Либо браузер показывает кешированную версию

## 📝 Важно понимать

**`data-page` с JSON - это нормально!** Это атрибут для гидратации на клиенте. HTML контент должен быть **внутри** тега `<div id="app">`, а не в атрибуте.

