# Решение проблемы: SSR не работает

## 🔴 Проблема

После сборки проекта с SSR (`npm run build:ssr`), в исходном коде страницы все еще видно только:
```html
<div id="app" data-page="{...json...}">
    <!-- Пусто, контент отсутствует -->
</div>
```

Это означает, что **SSR не активен**.

## ✅ Решение

### Вариант 1: Запуск SSR сервера через Laravel Artisan

Laravel Inertia требует запущенный SSR сервер для работы SSR:

```bash
# В Docker контейнере
docker-compose -f docker-compose.local.yml exec app php artisan inertia:start-ssr

# Или локально
php artisan inertia:start-ssr
```

Эта команда запустит SSR сервер, который будет обрабатывать запросы и рендерить HTML на сервере.

### Вариант 2: Проверка конфигурации

Убедитесь, что:

1. **SSR бандл собран:**
   ```bash
   ls -la laravel/bootstrap/ssr/ssr.js
   ```
   Файл должен существовать.

2. **Laravel видит SSR бандл:**
   Проверьте, что Laravel Vite Plugin правильно настроен в `vite.config.ts`:
   ```typescript
   laravel({
       input: 'resources/js/app.ts',
       ssr: 'resources/js/ssr.js', // Должно быть раскомментировано
       refresh: true,
   })
   ```

3. **Очистите кеш Laravel:**
   ```bash
   docker-compose -f docker-compose.local.yml exec app php artisan cache:clear
   docker-compose -f docker-compose.local.yml exec app php artisan config:clear
   docker-compose -f docker-compose.local.yml exec app php artisan view:clear
   ```

### Вариант 3: Использование внешнего SSR сервера

Если встроенный SSR через Laravel не работает, можно использовать внешний SSR сервер:

1. **Запустите SSR сервер:**
   ```bash
   cd laravel
   npm run ssr
   ```

2. **Обновите HandleInertiaRequests** для использования внешнего SSR сервера (см. документацию в `docs/SEO_SSR_ANALYSIS.md`)

## 🔍 Диагностика

### Проверка 1: SSR сервер запущен?

```bash
# Проверить процессы
ps aux | grep node | grep ssr

# Или проверить порт
netstat -an | grep 13714
```

### Проверка 2: SSR бандл существует?

```bash
ls -la laravel/bootstrap/ssr/ssr.js
```

### Проверка 3: Laravel использует SSR?

Проверьте логи Laravel на наличие ошибок SSR:
```bash
docker-compose -f docker-compose.local.yml exec app tail -f storage/logs/laravel.log
```

## 📝 Важные замечания

1. **SSR требует запущенного сервера** - просто сборки недостаточно
2. **SSR сервер должен работать постоянно** - запускайте его в фоновом режиме или через supervisor/PM2
3. **Производительность** - SSR увеличивает нагрузку на сервер

## 🎯 Быстрое решение

```bash
# 1. Убедитесь, что SSR бандл собран
cd laravel
npm run build:ssr

# 2. Очистите кеш Laravel
docker-compose -f docker-compose.local.yml exec app php artisan cache:clear

# 3. Запустите SSR сервер
docker-compose -f docker-compose.local.yml exec app php artisan inertia:start-ssr

# 4. Проверьте работу
curl http://localhost:8116/catalog | grep -i "каталог"
```

Если после выполнения этих шагов SSR все еще не работает, проверьте логи и убедитесь, что все зависимости установлены.

