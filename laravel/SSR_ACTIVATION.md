# Активация SSR для PrimeFoods Store

## 🔴 Проблема: SSR не работает

Если вы видите в исходном коде страницы только:
```html
<div id="app" data-page="{...json...}">
    <!-- Пусто, контент отсутствует -->
</div>
```

Это означает, что **SSR не активирован**.

## ✅ Решение: Активация SSR

### Шаг 1: Соберите проект с SSR

**ОБЯЗАТЕЛЬНО!** SSR работает только после сборки проекта с SSR:

```bash
cd laravel
npm run build:ssr
```

Эта команда:
- Соберет SSR entry point (`resources/js/ssr.js`)
- Создаст SSR бандл в `public/build/ssr/`
- Настроит Laravel Vite Plugin для использования SSR

### Шаг 2: Проверьте, что сборка прошла успешно

После сборки должны появиться файлы:
- `public/build/ssr/ssr.js` (или подобный)
- `public/build/manifest.json` (содержит информацию о SSR бандле)

### Шаг 3: Очистите кеш Laravel

```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

### Шаг 4: Проверьте работу SSR

Откройте страницу и посмотрите исходный код (View Page Source).

**✅ SSR работает, если видите:**
```html
<div id="app" data-page="{...json...}">
    <div class="catalog-page">
        <h1>Каталог</h1>
        <!-- Полный контент виден -->
    </div>
</div>
```

**❌ SSR не работает, если видите:**
```html
<div id="app" data-page="{...json...}">
    <!-- Пусто, контент отсутствует -->
</div>
```

## 🔧 Альтернативный способ: Использование SSR сервера

Если встроенный SSR через Laravel Vite Plugin не работает, можно использовать внешний SSR сервер:

### Шаг 1: Запустите SSR сервер

```bash
cd laravel
npm run ssr
```

SSR сервер будет доступен на порту 13714 (или порту из `SSR_PORT`).

### Шаг 2: Обновите HandleInertiaRequests

Добавьте логику для использования внешнего SSR сервера в `app/Http/Middleware/HandleInertiaRequests.php`:

```php
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

public function handle(Request $request, Closure $next): Response
{
    $response = $next($request);
    
    // Если это поисковый робот, используем SSR
    if ($request->attributes->get('is_search_engine_bot', false)) {
        // Здесь можно добавить логику для SSR через внешний сервис
        // Например, запрос к SSR серверу на порту 13714
    }
    
    return $response;
}
```

## 🐛 Решение проблем

### Проблема 1: SSR не работает после сборки

**Решение:**
1. Убедитесь, что сборка прошла без ошибок
2. Проверьте, что файлы SSR бандла созданы в `public/build/ssr/`
3. Очистите кеш Laravel
4. Перезапустите сервер разработки

### Проблема 2: Ошибки при сборке SSR

**Решение:**
1. Проверьте `resources/js/ssr.js` на ошибки
2. Убедитесь, что все зависимости установлены (`npm install`)
3. Проверьте логи сборки на ошибки

### Проблема 3: SSR работает только для ботов

**Решение:**
SSR должен работать для всех запросов после сборки. Если работает только для ботов, проверьте:
1. Правильно ли настроен `DetectSearchEngineBot` middleware
2. Не блокирует ли что-то SSR для обычных пользователей

## 📝 Важные замечания

1. **SSR требует сборки** - не работает автоматически в режиме разработки
2. **Производительность** - SSR увеличивает время ответа сервера
3. **Кеширование** - рекомендуется использовать кеширование для SSR страниц
4. **Мониторинг** - следите за производительностью после включения SSR

## 🎯 Быстрая проверка

```bash
# 1. Собрать с SSR
npm run build:ssr

# 2. Очистить кеш
php artisan cache:clear

# 3. Проверить исходный код страницы
curl http://localhost/catalog | grep -i "каталог"
```

Если команда возвращает результат - SSR работает! ✅

